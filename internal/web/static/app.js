(() => { "use strict";
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)],COMPARE="/api/trajectory-comparison";
const state={catalog:{harnesses:[],cases:[],benchmarks:[]},attempts:new Map(),filter:"all",playback:null,compare:new Set(),deleteRuns:new Set()};
const active=s=>["queued","running"].includes(s), deepSeek=a=>/dsh|deepseek/i.test(`${a.harness_id||a.variant_id||""} ${a.harness_name||a.display_name||a.name||""}`), captured=a=>a.trajectory_available===true, dsh=captured, el=(t,v,c)=>{let e=document.createElement(t);e.textContent=v;if(c)e.className=c;return e}, n=v=>typeof v==="number"?v.toLocaleString():"—", dur=v=>typeof v==="number"&&v>=0?`${Math.floor(v/60000)?Math.floor(v/60000)+"m ":""}${Math.floor(v/1000)%60}s`:"—", use=a=>a.result?.usage||a.usage||{}, verdict=a=>{let v=a.verdict||a.result?.verdict;if(v==="pass"||v==="fail")return v;let p=a.verifier_passed??a.result?.verifier_passed;return p===true?"pass":p===false?"fail":""}, verdictLabel=a=>{let v=verdict(a);if(v)return v==="pass"?"Pass":"Fail";if(a.status==="running")return"Running";if(a.status==="queued")return"Queued";return"Error"};
async function api(path,o){let r=await fetch(path,{headers:o?.body instanceof FormData?{}:{"Content-Type":"application/json"},...o});if(!r.ok){let x=`Request failed (${r.status})`;try{x=(await r.json()).error||x}catch(_){}throw Error(x)}return r.status===204?null:r.json()}
async function removeRun(a){let id=a.batch_id;if(!id||active(a.status))return;if(!confirm("Delete this run and all of its saved artifacts? This cannot be undone."))return;try{await api(`/api/runs/${encodeURIComponent(id)}`,{method:"DELETE"});for(let [key,item] of state.attempts)if(item.batch_id===id){state.attempts.delete(key);state.compare.delete(key)}attempts();comparisonControls()}catch(e){alert(e.message)}}
function removeBatches(ids){for(let id of ids)for(let [key,item] of state.attempts)if(item.batch_id===id){state.attempts.delete(key);state.compare.delete(key)}state.deleteRuns.clear();attempts();comparisonControls()}
function bulkControls(){let button=$("#delete-selected"),count=state.deleteRuns.size;button.disabled=!count;button.textContent=count?`Delete selected (${count})`:"Delete selected"}
async function removeSelected(){let ids=[...state.deleteRuns];if(!ids.length)return;if(!confirm(`Delete ${ids.length} selected run${ids.length===1?"":"s"} and all saved artifacts? This cannot be undone.`))return;try{await api("/api/runs",{method:"DELETE",body:JSON.stringify({ids})});removeBatches(ids)}catch(e){alert(e.message)}}
const picks=()=>$$('#harness-list input:checked').map(x=>x.value), cases=()=>$$('#case-list input:checked').map(x=>x.value), benchmarkTasks=()=>$$('#benchmark-list input:checked').map(x=>x.value), models=h=>(h.supported_models?.map(x=>x.id||x.model)||h.capabilities?.models||h.models||h.available_models||(h.model?[h.model]:[])), efforts=(h,m)=>{let supported=h.supported_models?.find(x=>(x.id||x.model)===m);if(supported)return supported.reasoning_efforts||[];let x=h.capabilities?.reasoning_efforts_by_model||h.reasoning_efforts_by_model||h.reasoning_efforts;return Array.isArray(x)?x:(x?.[m]||(h.model===m&&h.reasoning_effort?[h.reasoning_effort]:[]))};
function option(select,values,placeholder,preferred){let old=select.value,selected=values.includes(old)?old:(values.includes(preferred)?preferred:values[0]);select.replaceChildren();if(!values.length)select.append(el("option",placeholder));values.forEach(v=>{let o=el("option",v);o.value=v;o.selected=v===selected;select.append(o)});select.disabled=!values.length}
function configs(){let hs=state.catalog.harnesses.filter(x=>picks().includes(x.id)),m=$("#model-select"),r=$("#reasoning-select"),sets=hs.map(models).filter(x=>x.length),ms=hs.length&&sets.length?sets[0].filter(x=>sets.every(y=>y.includes(x))):[];option(m,ms,hs.length?"No compatible models":"Select a harness",hs[0]?.model);let es=hs.map(x=>efforts(x,m.value)).filter(x=>x.length),rs=es.length?es[0].filter(x=>es.every(y=>y.includes(x))):[];option(r,rs,hs.length?"No compatible reasoning levels":"Select a model",hs[0]?.reasoning_effort);launch()}
function harness(h,checked){let l=document.createElement("label"),i=document.createElement("input"),s=document.createElement("span");l.className=`harness-choice${h.available===false?" disabled":""}`;i.type="checkbox";i.value=h.id;i.checked=checked;i.disabled=h.available===false;i.onchange=configs;s.append(el("b",h.display_name||h.name||h.id),el("small",h.description||models(h).join(", ")||"No model available"),el("em",captured(h)?"Trajectory telemetry + playback":"Trajectory unavailable",`telemetry-note${captured(h)?"":" unavailable"}`));l.append(i,s);return l}
function choice(c,checked){let l=document.createElement("label"),i=document.createElement("input"),s=document.createElement("span");l.className="choice";i.type="checkbox";i.value=c.id;i.checked=checked;i.onchange=launch;s.append(el("b",c.name||c.id),el("small",c.prompt_preview||c.description||c.id));l.append(i,s);return l}
function tokenTier(task){return task.token_tier||task.token_budget||task.provenance?.token_tier||task.provenance?.token_budget||"Token tier unavailable"}
function benchmarkSuite(suite,selected){let section=document.createElement("section"),heading=document.createElement("div"),tasks=Array.isArray(suite.tasks)?suite.tasks:[],count=suite.task_count??tasks.length,tiers=tasks.length?[...new Set(tasks.map(tokenTier))]:["Token tier unavailable"];section.className="benchmark-suite";heading.className="benchmark-suite-heading";heading.append(el("h3",suite.name||suite.id||"Benchmark"),el("p",suite.description||"Benchmark task suite."));let meta=el("p",`${count} task${count===1?"":"s"} · ${tiers.join(", ")}`,"benchmark-meta");section.append(heading,meta);let list=document.createElement("div");list.className="benchmark-task-list";for(let task of tasks){let label=document.createElement("label"),input=document.createElement("input"),copy=document.createElement("span"),name=task.name||task.title||task.task_id||task.id;label.className="benchmark-task";input.type="checkbox";input.value=task.id;input.checked=selected.has(task.id);input.onchange=launch;copy.append(el("b",name),el("small",`${tokenTier(task)}${task.description?" · "+task.description:""}`));label.append(input,copy);list.append(label)}section.append(tasks.length?list:el("p","No runnable tasks are available in this suite.","empty"));return section}
function catalog(){let h=new Set(picks()),c=new Set(cases()),b=new Set(benchmarkTasks()),order={"dsh-default-codex":0,"dsh-modified-codex":1,"codex-cli":2},hs=state.catalog.harnesses.filter(x=>x.id in order).sort((a,b)=>order[a.id]-order[b.id]);$("#harness-list").replaceChildren(...hs.map(x=>harness(x,h.has(x.id))));let cs=state.catalog.cases.map(x=>choice(x,c.has(x.id)));$("#case-list").replaceChildren(...(cs.length?cs:[el("p","No test cases found. Import one to get started.","empty")]));let suites=Array.isArray(state.catalog.benchmarks)?state.catalog.benchmarks:[];$("#benchmark-list").replaceChildren(...(suites.length?suites.map(x=>benchmarkSuite(x,b)):[el("p","No benchmark suites are available.","empty")]));configs()}
function launch(){let h=picks().length,local=cases().length,benchmark=benchmarkTasks().length,selected=local+benchmark,count=h*selected,m=$("#model-select"),r=$("#reasoning-select"),b=$("#start-run"),item=selected===1?"selection":"selections";$("#attempt-count").textContent=`${count} attempt${count===1?"":"s"}`;b.disabled=!count||m.disabled||r.disabled;$("#launch-summary").textContent=count?`${h} harness${h===1?"":"es"} × ${selected} ${item} (${local} local case${local===1?"":"s"}, ${benchmark} benchmark task${benchmark===1?"":"s"}) = exactly ${count} attempts at ${m.value} / ${r.value}.`:"Select a harness and a local test case or benchmark task to continue."}
function merge(a){if(!a?.id)return;let old=state.attempts.get(a.id)||{};state.attempts.set(a.id,{...old,...a,harness_id:a.harness_id||a.variant_id||old.harness_id,elapsed_ms:a.elapsed_ms??a.elapsed_millis??old.elapsed_ms,usage:{...old.usage,...a.usage},logs:a.logs||old.logs||[]});attempts();comparisonControls()}
function stat(node,k,v){$(`[data-stat="${k}"]`,node).textContent=v}
function humanValue(v){if(v===null||v===undefined)return"—";if(typeof v!=="object")return String(v);return Object.entries(v).map(([k,x])=>`${k.replace(/_/g," ")}: ${humanValue(x)}`).join(" · ")}
function caseDetails(a){if(!a.benchmark_id)return null;let box=document.createElement("section");box.className="deterministic-case";box.append(el("h4","Deterministic test case"));let list=document.createElement("dl"),entries=[["Benchmark",a.benchmark_id],["Test case",a.benchmark_task_id||a.case_id],["Status",verdictLabel(a)],["Evaluator",a.evaluator||a.provenance?.evaluator||"Deterministic verifier"],["Provenance",typeof a.provenance==="string"?a.provenance:a.provenance?.source]];entries.filter(([,v])=>v).forEach(([k,v])=>{let row=document.createElement("div");row.append(el("dt",k),el("dd",humanValue(v),k==="Status"?`test-verdict ${verdict(a)||a.status}`:""));list.append(row)});box.append(list);return box}
function benchmarkRuns(all){let groups=new Map;all.filter(a=>a.benchmark_id).forEach(a=>{let id=a.batch_id||"unbatched";if(!groups.has(id))groups.set(id,[]);groups.get(id).push(a)});let host=$("#benchmark-runs");host.replaceChildren(...[...groups.entries()].map(([id,items])=>{let card=document.createElement("article"),head=document.createElement("div"),details=document.createElement("div"),button=el("button","Details","secondary");card.className="benchmark-run card";details.hidden=true;head.className="benchmark-run-head";let passed=items.filter(a=>verdict(a)==="pass").length,failed=items.filter(a=>verdict(a)==="fail").length;head.append(el("div",`Benchmark run ${id}`),el("small",`${items.length} deterministic attempt${items.length===1?"":"s"} · ${passed} pass · ${failed} fail`),button);let list=document.createElement("ul");list.className="benchmark-run-cases";items.sort((a,b)=>String(a.case_id).localeCompare(String(b.case_id))).forEach(a=>{let row=document.createElement("li");row.append(el("strong",a.benchmark_task_id||a.case_id),el("span",a.harness_name||a.display_name||a.variant_id||"Harness"),el("span",verdictLabel(a),`test-verdict ${verdict(a)||a.status}`));list.append(row)});details.append(list);button.onclick=()=>{details.hidden=!details.hidden;button.textContent=details.hidden?"Details":"Hide details"};card.append(head,details);return card}))}
function card(a){let x=$("#attempt-template").content.firstElementChild.cloneNode(true),u=use(a),r=a.result||{},s=a.status||"queued",v=verdict(a),name=a.harness_name||a.display_name||a.harness_id||a.variant_id||"Harness",captured=dsh(a)&&s==="completed";$(".status",x).classList.add(v||s);$(".attempt-title",x).textContent=`${name} · ${a.case_name||a.case_id||"Test case"}`;$(".attempt-subtitle",x).textContent=`${active(s)?s:(v?`finished · ${v}`:s)}${a.id?" · "+a.id:""}`;let badges=$(".attempt-badges",x);if(v)badges.append(el("span",`Deterministic verifier: ${verdictLabel(a)}`,`badge verdict-${v}`));badges.append(el("span",dsh(a)?(captured?"DSH trajectory captured":"DSH trajectory enabled"):"Trajectory unavailable",`badge ${dsh(a)?"trajectory-ready":"trajectory-off"}`));if(captured){let pick=document.createElement("label"),i=document.createElement("input");pick.className="compare-pick";i.type="checkbox";i.checked=state.compare.has(a.id);i.onchange=()=>{if(i.checked){if(state.compare.size===2){i.checked=false;return}state.compare.add(a.id)}else state.compare.delete(a.id);comparisonControls()};pick.append(i,document.createTextNode(" Compare"));badges.append(pick)}stat(x,"elapsed",dur(active(s)&&a.started_at?Date.now()-new Date(a.started_at):a.elapsed_ms));stat(x,"input-tokens",n(u.input_tokens));stat(x,"output-tokens",n(u.output_tokens));stat(x,"cached-input-tokens",n(u.cached_input_tokens));stat(x,"reasoning-output-tokens",n(u.reasoning_output_tokens));stat(x,"result",v?verdictLabel(a):s);stat(x,"harness",name);stat(x,"model",r.model||a.model||"—");stat(x,"reasoning-effort",r.reasoning_effort||a.reasoning_effort||"—");stat(x,"token-quality",u.token_quality||"—");stat(x,"exit-code",typeof r.exit_code==="number"?r.exit_code:"—");let d=$(".attempt-details",x),t=$(".details",x);t.onclick=()=>{d.hidden=!d.hidden;t.setAttribute("aria-expanded",String(!d.hidden));t.firstChild.textContent=d.hidden?"Details ":"Hide details "};if(!active(s)){let b=el("button","Delete run","secondary");b.type="button";b.onclick=()=>removeRun(a);$(".attempt-actions",x).prepend(b)}let deterministic=caseDetails(a);if(deterministic)d.prepend(deterministic);$(".log",x).textContent=a.logs?.join("\n")||"Raw provider output is not retained. Status, elapsed time, and provider usage appear above.";let err=a.error||a.failure_reason||r.failure_reason;if(err){let f=$(".failure",x);f.hidden=false;f.textContent=err}if(captured){let b=$(".trajectory-button",x);b.hidden=false;b.onclick=()=>trajectory(a)}return x}
function attempts(){let all=[...state.attempts.values()].sort((a,b)=>new Date(b.created_at||b.started_at||b.finished_at||0)-new Date(a.created_at||a.started_at||a.finished_at||0)),shown=all.filter(a=>state.filter==="all"||(state.filter==="active"?active(a.status):!active(a.status))),host=$("#attempts");benchmarkRuns(all);host.replaceChildren(...(shown.length?shown.map(card):[el("div",all.length?"No matching attempts":"No attempts yet","empty-state")]));let run=all.filter(a=>active(a.status)),fin=all.filter(a=>!active(a.status)),failed=fin.filter(a=>verdict(a)==="fail"||["failed","timed_out","infrastructure_invalid","unsupported","interrupted"].includes(a.status)),sum=k=>{let v=all.map(use).map(x=>x[k]).filter(x=>typeof x==="number");return v.length?n(v.reduce((a,b)=>a+b)):"—"},totals=all.map(use).map(x=>typeof x.input_tokens==="number"&&typeof x.output_tokens==="number"?x.input_tokens+x.output_tokens:null).filter(x=>x!==null),elapsed=all.map(a=>active(a.status)&&a.started_at?Date.now()-new Date(a.started_at):a.elapsed_ms).filter(x=>typeof x==="number");$("#metric-running").textContent=run.length;$("#metric-completed").textContent=fin.length;$("#metric-passed").textContent=fin.filter(a=>verdict(a)==="pass").length;$("#metric-failed").textContent=failed.length;$("#metric-input-tokens").textContent=sum("input_tokens");$("#metric-output-tokens").textContent=sum("output_tokens");$("#metric-tokens").textContent=totals.length?n(totals.reduce((a,b)=>a+b)):"—";$("#metric-elapsed").textContent=elapsed.length?dur(Math.max(...elapsed)):"—";$("#filter-all").textContent=all.length;$("#filter-active").textContent=run.length;$("#filter-finished").textContent=fin.length;bulkControls()}
async function start(){let b=$("#start-run");b.disabled=true;b.textContent="Starting…";try{let r=await api("/api/runs",{method:"POST",body:JSON.stringify({harness_ids:picks(),case_ids:cases(),benchmark_task_ids:benchmarkTasks(),model:$("#model-select").value,reasoning_effort:$("#reasoning-select").value})});(r.attempts||[]).forEach(merge)}catch(e){alert(e.message)}finally{b.textContent="Start benchmark ↗";launch()}}
const eventName=e=>e?.label||e?.name||e?.type||e?.category||"event", eventType=e=>e?.type||e?.category||"event";
function eventLane(e){let exact=`${e?.type||""} ${e?.label||""}`.toLowerCase(),v=`${exact} ${e?.category||""}`.toLowerCase();if(/tool|function|command|shell|browser|exec/.test(exact))return"tools";if(/request.*header|header.*request/.test(exact))return"model";if(/user|context/.test(exact))return"input";if(/assistant|model|response|completion|reasoning|llm/.test(exact))return"model";if(/request|input|message/.test(exact))return"input";if(/tool|function|command|shell|browser|exec/.test(v))return"tools";return /assistant|model|response|completion|reasoning|llm|header/.test(v)?"model":"input"}
function eventTime(e,i){let v=e?.relative_ms??e?.elapsed_ms??e?.offset_ms;if(typeof v==="number")return v;let t=Date.parse(e?.timestamp||e?.time||"");return Number.isFinite(t)?t:i}
function displayValue(v){return typeof v==="string"?v:JSON.stringify(v,null,2)}
function eventDetails(e){let host=$("#trajectory-event-details");host.replaceChildren();if(!e){host.append(el("p","No events match this filter.","empty"));return}for(let [key,value] of Object.entries(e)){if(["type","category","label","name","relative_ms","elapsed_ms","timestamp","time"].includes(key)||value===undefined)continue;let section=document.createElement("section"),heading=el("h4",key.replace(/_/g," ")),pre=el("pre",displayValue(value));section.append(heading,pre);host.append(section)}}
async function trajectory(a){let d=$("#trajectory-dialog");d.showModal();$("#trajectory-title").textContent=`${a.harness_name||a.display_name||a.harness_id||a.variant_id||"DeepSeek Harness"} trajectory`;try{let data=await api(`/api/attempts/${encodeURIComponent(a.id)}/trajectory`),events=Array.isArray(data)?data:(data.events||data.trajectory||[]);state.playback={events,visible:events.map((_,i)=>i),index:0,timer:null};let f=$("#trajectory-filter");f.replaceChildren(el("option","All event types"));f.firstChild.value="";[...new Set(events.map(eventType))].forEach(v=>{let o=el("option",v);o.value=v;f.append(o)});$("#trajectory-summary").textContent=`${events.length} recorded events · ${dur(data.duration_ms??a.elapsed_ms)} elapsed`;paint()}catch(e){state.playback={events:[],visible:[],index:0,timer:null};$("#trajectory-summary").textContent=`Trajectory unavailable: ${e.message}`;paint()}}
function paint(){let p=state.playback,e=p?.events[p.visible[p.index]];if(!p)return;let lanes=$("#trajectory-lanes"),times=p.events.map(eventTime),start=Math.min(...times,0),end=Math.max(...times,start+1),span=Math.max(1,end-start);lanes.replaceChildren(...p.events.map((event,id)=>{let b=document.createElement("button"),at=eventTime(event,id),next=eventTime(p.events[id+1],id+1),width=Math.max(1,Math.min(100,((Math.max(at+1,next)-at)/span)*100));b.type="button";b.className=`trajectory-block ${eventLane(event)}${p.visible[p.index]===id?" selected":""}${p.visible.includes(id)?"":" filtered"}`;b.style.left=`${Math.max(0,Math.min(99,(at-start)/span*100))}%`;b.style.width=`${width}%`;b.title=`${eventName(event)} · ${eventType(event)}${typeof at==="number"?` · ${dur(at)}`:""}`;b.setAttribute("aria-label",b.title);b.onclick=()=>{p.index=p.visible.indexOf(id);if(p.index>=0)paint()};return b}));$("#trajectory-events").replaceChildren(...p.visible.map((id,i)=>{let event=p.events[id],x=el("li","",i===p.index?"current":"");x.append(el("strong",`${i+1}. ${eventName(event)}`),el("small",`${eventType(event)} · ${eventLane(event)} · ${dur(eventTime(event,id))}`));x.onclick=()=>{p.index=i;paint()};return x}));let when=e&&eventTime(e,p.visible[p.index]);let meta=$("#trajectory-event-meta");meta.replaceChildren();if(e){[["Event",eventName(e)],["Type",eventType(e)],["Lane",eventLane(e)],["Offset",typeof when==="number"?dur(when):"—"],["Recorded",e.timestamp||e.time||"—"]].forEach(([k,v])=>{let row=document.createElement("div");row.append(el("dt",k),el("dd",String(v)));meta.append(row)})}else meta.append(el("div","No matching events"));eventDetails(e)}
function move(n){let p=state.playback;if(p?.visible.length){p.index=Math.max(0,Math.min(p.visible.length-1,p.index+n));paint()}}
function playback(){let d=$("#trajectory-dialog"),play=$("#trajectory-play");$("[data-close-trajectory]",d).onclick=()=>d.close();d.onclose=()=>{if(state.playback?.timer)clearInterval(state.playback.timer);state.playback=null;play.textContent="Play"};$("#trajectory-prev").onclick=()=>move(-1);$("#trajectory-next").onclick=()=>move(1);$("#trajectory-filter").onchange=e=>{let p=state.playback;if(!p)return;p.visible=p.events.map((v,i)=>({v,i})).filter(x=>!e.target.value||eventType(x.v)===e.target.value).map(x=>x.i);p.index=0;paint()};play.onclick=e=>{let p=state.playback;if(!p?.visible.length)return;if(p.timer){clearInterval(p.timer);p.timer=null;e.target.textContent="Play"}else{e.target.textContent="Pause";p.timer=setInterval(()=>{if(p.index>=p.visible.length-1){e.target.click()}else move(1)},+$("#trajectory-speed").value)}}}
function comparisonControls(){let ok=[...state.attempts.values()].filter(a=>dsh(a)&&a.status==="completed");for(let id of [...state.compare])if(!ok.some(a=>a.id===id))state.compare.delete(id);$("#compare-trajectories").disabled=state.compare.size!==2;$("#comparison-help").textContent=ok.length?"Select two completed DeepSeek Harness attempts for the same test case.":"No completed DeepSeek Harness trajectories are available yet."}
function compareView(name,events){let view=document.createElement("section"),lanes=document.createElement("div"),log=document.createElement("ol"),inspector=document.createElement("pre"),selected=0;view.className="compare-trajectory-view";view.append(el("h3",name));lanes.className="compare-lanes";log.className="compare-event-log";function paintView(){let times=events.map(eventTime),start=Math.min(...times,0),span=Math.max(1,Math.max(...times,start+1)-start);lanes.replaceChildren(...events.map((event,i)=>{let b=document.createElement("button"),at=eventTime(event,i);b.type="button";b.className=`trajectory-block ${eventLane(event)}${i===selected?" selected":""}`;b.style.left=`${Math.max(0,Math.min(99,(at-start)/span*100))}%`;b.style.width=`${Math.max(2,Math.min(100,100/events.length))}%`;b.onclick=()=>{selected=i;paintView()};return b}));log.replaceChildren(...events.map((event,i)=>{let row=el("li",`${i+1}. ${eventName(event)} · ${eventType(event)}`,i===selected?"current":"");row.onclick=()=>{selected=i;paintView()};return row}));inspector.textContent=events[selected]?JSON.stringify(events[selected],null,2):"No event"}view.append(lanes,log,inspector);paintView();return view}
async function compare(){let [l,r]=[...state.compare],a=state.attempts.get(l),b=state.attempts.get(r);if(a?.case_id!==b?.case_id){$("#comparison-result").replaceChildren(el("p","Choose attempts for the same test case.","form-error"));return}try{let x=await api(`${COMPARE}?left=${encodeURIComponent(l)}&right=${encodeURIComponent(r)}`),rows=x.rows||x.events||[],leftName=a.display_name||a.variant_id||"Left",rightName=b.display_name||b.variant_id||"Right",host=document.createElement("div"),views=document.createElement("div"),differences=document.createElement("section"),changed=rows.filter(v=>!(v.same??v.match));host.className="comparison-pairs";views.className="compare-trajectory-views";views.append(compareView(leftName,x.left_raw_events||rows.map(v=>v.left).filter(Boolean)),compareView(rightName,x.right_raw_events||rows.map(v=>v.right).filter(Boolean)));differences.className="comparison-differences";differences.append(el("h3",changed.length?"Aligned behavioral differences":"No behavioral differences"));changed.forEach((v,i)=>{let item=document.createElement("article"),list=document.createElement("ul");item.append(el("strong",`Behavioral event ${i+1}: ${eventName(v.left)||"missing"} → ${eventName(v.right)||"missing"}`));(v.differences||[]).forEach(d=>list.append(el("li",`${d.field||"event"}: ${displayValue(d.left)} → ${displayValue(d.right)}`)));item.append(list.childElementCount?list:el("p","The event is present on only one side.","empty"));differences.append(item)});if(!changed.length)differences.append(el("p","No behavioral differences. Timing, request metadata, streaming chunks, and usage are ignored for this comparison.","empty"));host.append(views,differences);$("#comparison-result").replaceChildren(el("p",x.comparison?.summary||`${rows.length} aligned behavioral events.`,"comparison-summary"),host)}catch(e){$("#comparison-result").replaceChildren(el("p",e.message,"form-error"))}}
async function load(){try{let [c,r]=await Promise.all([api("/api/catalog"),api("/api/runs")]);state.catalog=c;(Array.isArray(r)?r:[r]).flatMap(x=>x?.attempts||[]).forEach(merge);catalog();attempts();$("#connection").className="connection online";$("#connection span").textContent="Live"}catch(e){$("#connection").className="connection offline"}}
function stream(){if(!window.EventSource)return;let s=new EventSource("/api/events"),connection=$("#connection");s.onopen=()=>{connection.className="connection online";$("span",connection).textContent="Live"};s.onerror=()=>{connection.className="connection offline";$("span",connection).textContent="Reconnecting"};["attempt","progress"].forEach(t=>s.addEventListener(t,e=>{try{merge(JSON.parse(e.data).attempt)}catch(_){}}));s.addEventListener("batch-deleted",e=>{try{let id=JSON.parse(e.data).batch_id;for(let [key,item] of state.attempts)if(item.batch_id===id){state.attempts.delete(key);state.compare.delete(key)}attempts();comparisonControls()}catch(_){}});s.addEventListener("log",e=>{try{let x=JSON.parse(e.data),a=state.attempts.get(x.attempt_id);if(a){a.logs=[...(a.logs||[]),x.line];attempts()}}catch(_){}})}
function imports(){let d=$("#import-dialog"),f=$("#import-form");$("#show-import").onclick=()=>d.showModal();$$('[data-close-dialog]',d).forEach(b=>b.onclick=()=>d.close());$$('input[name="source_type"]',f).forEach(i=>i.onchange=()=>["archive","files","repo"].forEach(x=>$(`#${x}-field`,f).hidden=i.value!==(x==="repo"?"repo_url":x)));f.onsubmit=async e=>{e.preventDefault();let data=new FormData(f),error=$("#import-error",f);data.set("id",data.get("name"));try{let out=await api("/api/cases",{method:"POST",body:data}),item=out.case||out;state.catalog.cases.push({...item,name:item.name||item.id,prompt_preview:item.prompt_preview||item.prompt});catalog();d.close();f.reset()}catch(x){error.textContent=x.message}}}
$("#start-run").onclick=start;$("#refresh").onclick=load;$("#model-select").onchange=configs;$("#reasoning-select").onchange=launch;$$('.filter').forEach(b=>b.onclick=()=>{state.filter=b.dataset.filter;$$('.filter').forEach(x=>x.classList.toggle("active",x===b));attempts()});$("#compare-trajectories").onclick=compare;imports();playback();load();stream();setInterval(()=>{if([...state.attempts.values()].some(a=>active(a.status)))attempts()},1000);
})();
